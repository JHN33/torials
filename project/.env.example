# Frontend uses this while running `npm run dev`.
VITE_API_URL=http://localhost:4173

# Backend settings.
PORT=4173
JWT_SECRET=replace-with-a-long-random-secret

# Owner-only publishing account. Change these for production.
OWNER_EMAIL=owner@torials.local
OWNER_PASSWORD=Torials123
